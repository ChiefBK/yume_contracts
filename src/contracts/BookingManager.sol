// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.0;

import './structs/Booking.sol';

contract BookingManager {
    mapping(uint => Booking) listings;

    function createBooking(uint _listingId) public {

    }
}
